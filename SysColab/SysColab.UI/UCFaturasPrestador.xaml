<UserControl x:Class="SysColab.UI.UCFaturasPrestador"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SysColab.UI"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="480" d:DesignWidth="900" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Background="LightGray">

    <Grid Margin="20 0">
        <Grid.RowDefinitions>
            <RowDefinition Height="60"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="NomePrestador" Grid.Row="0" Style="{StaticResource MaterialDesignDisplay1TextBlock}" Foreground="Gray" FontSize="60" VerticalAlignment="Center" Margin="10 0 0 0"/>
        <!--<Button Style="{StaticResource MaterialDesignFloatingActionMiniLightButton}" VerticalAlignment="Center" Grid.Column="1" Margin="-24 0 0 0">
            <materialDesign:PackIcon Kind="ArrowForward"/>
        </Button>-->
      
    </Grid>
</UserControl>
